networks:
  celine_dev:

services:
  celine_proxy:
    image: caddy:2.8
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./config/caddy/Caddyfile:/etc/caddy/Caddyfile:ro
    networks:
      - celine_dev
    extra_hosts:
      - "host-gw:host-gateway"
      - "host.docker.internal:host-gateway"
      - "keycloak.celine.localhost:host-gateway"
      - "sso.celine.localhost:host-gateway"
